{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild src/main.js --sourcemap --bundle --outfile=dist/dist.js --watch",
    "build": "esbuild src/main.js --minify --bundle --outfile=dist/dist.js",
    "build-css": "esbuild --minify --bundle src/style.css --outfile=dist/dist.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.2"
  }
}
